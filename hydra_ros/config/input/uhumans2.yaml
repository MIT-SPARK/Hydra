---
input:
  type: RosInput
  tf_verbosity: 1
  clear_queue_on_fail: true
  verbosity: 2
  inputs:
    left_cam:
      receiver: {type: ImageReceiver, semantics_type: closed_set, verbosity: 1}
      sensor: {type: camera_info, min_range: 0.1, max_range: 5.0, extrinsics: {type: ros, robot_frame: base_link_gt}}
